# API Reference

This page is generated directly from the source-code docstrings.

Use it when you want exact function signatures, argument definitions, keyword
defaults, and return conventions. For a narrative explanation of the workflows,
start with the guide pages — [Constructing Groups](groups.md),
[Character Tables](character-tables.md), and [Representations](representations.md).

```@autodocs
Modules = [FiniteGroups]
Private = false
Order = [:type, :function]
```
